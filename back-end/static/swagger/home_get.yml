API Health Check
  ---
  tags:
      - Home API
  description: Returns a welcome message for the Flask API home route
  responses:
      200:
          description: Welcome message retrieved successfully
          schema:
              type: object
              properties:
                  status:
                      type: boolean
                      example: true
                      description: Status of the response
                  message:
                      type: string
                      example: Welcome to Flask API
                      description: Welcome message
